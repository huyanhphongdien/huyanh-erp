node_modules

# Build output (auto-generated by vite)
dist/
apps/weighbridge/dist/

# Weighbridge: extracted bundle working dir — distribute via camera-proxy-win32.zip
apps/weighbridge/camera-proxy-win32/

# Scratch / session-local artifacts
.tmp/
.claude/settings.local.json

# Accidental nested clone
huyanh-erp-8/

# Test render outputs (sinh từ docs/contract-templates/test_render.mjs)
docs/contract-templates/_out_*.docx

# Secrets — KHONG BAO GIO commit
.env.local
.env.*.local
*.key

# ── Dữ liệu nhạy cảm / file thô — KHÔNG commit (khách hàng, backup, ảnh, archive) ──
# (đã từng bị lỡ `git add -A` cuốn vào commit + push — repo từng public)
backup_*.sql
*.rar
*.zip
# Dữ liệu thô: hợp đồng khách, ảnh cân, tài liệu tham chiếu
docs/du lieu tho/
SC + PI*/
apps/weighbridge/*.png
apps/weighbridge/*.jpg
